#include<iostream>
#include<string.h>
using namespace std;
int main(){
   string str;
   getline(cin,str);
   cout<<"Length of string is "<<str.length();
   return 0;
   
  /*
   char str[]="";
   gets(str);
   cout<<strlen(str);
  */
}
